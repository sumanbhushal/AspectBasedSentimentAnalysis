
def compactness_pruning():
    pass


def redundancy_pruning(sentences, terms):
    # for s in sentences do
    #     for t: t in terms ∧ t in s do
    #         if ¬∃ t’: t’ in terms ∧ t’ in s ∧ t in t’ then
    #         psupport[term] += 1
    for s in sentences:
        pass

print("this is bla bla".split())